# Workspace deps hoist to the repo root; this only appears if npm has to keep a
# conflicting version local to the package.
node_modules

# Test scratch: compiled core (npm test), ingest smoke output, generated PSD/PDF
# fixtures. All reproducible with `node test/make-fixtures.cjs`.
test/.build/
test/.out/
test/.fixtures/

.DS_Store
*.tsbuildinfo
